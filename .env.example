# Copiar a .env y completar. Ver docs/03-arquitectura.md.
# Base de datos PostgreSQL (local: docker-compose.yml incluido; producción: crédito de nube para ONG).
DATABASE_URL="postgresql://cognos:cognos@localhost:5432/cognos?schema=public"
