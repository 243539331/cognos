services:
  db:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: cognos
      POSTGRES_PASSWORD: cognos
      POSTGRES_DB: cognos
    ports:
      - "5432:5432"
    volumes:
      - cognos-db:/var/lib/postgresql/data

volumes:
  cognos-db:
